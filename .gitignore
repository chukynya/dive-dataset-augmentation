# --- Large raw data (keep out of git) ---
Dataset/Bytecode_filled.csv
Dataset/Transaction-based.csv
Dataset/DIVE_Labels.csv
Dataset/*.bak

# --- Large corpora / generated output trees ---
Dataset/Source/
Dataset/buggy/
Dataset/final/

# --- Large regenerable injection cache (rebuilt by the pipeline) ---
work/cache/synthetic_bytecode.csv
work/cache/real_canon_bytecode.json
work/cache/train_pragma.json
work/cache/injection_jobs.json
work/cache/injection_failed.json
work/cache/all_split_assignments.json
work/cache/run.log

# --- Large dataset zips ---
Dataset/*.zip
Dataset/aug_data.zip

# --- Upstream tool clone (reproducible via work/scripts) ---
work/SolidiFI/

# --- Large binaries tracked via Git LFS (see .gitattributes) ---
# Dataset.zip (~363MB) and solidifi-multi.tar (~184MB) are stored in Git LFS,
# so `git clone` (with git-lfs installed) pulls them automatically.

# --- Python caches ---
__pycache__/
*.pyc

# --- Local virtualenv ---
.venv/

